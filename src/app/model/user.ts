export abstract class User {
    name: string;
	emailId: string;
	contactNumber: string;
	userId: string;
	userType: string;
	recordLastUpdatedMillis: number;
	updatedBy: string;
    updatedByName: string;
}